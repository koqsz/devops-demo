apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  labels:
    app: backend
data:
  NODE_ENV: "production"
  PORT: "3000"
  DB_HOST: "database"
  DB_PORT: "5432"
  DB_NAME: "taskmanager"
